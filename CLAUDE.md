@AGENTS.md

## 언어

**모든 출력은 한국어로 쓴다.** 예외 없다.

- 대화·설명·요약·질문
- 코드 주석
- 커밋 메시지
- 문서

코드 자체(식별자, 문자열 키, API 이름)는 원래 쓰던 대로 둔다. 영어 기술 용어를
한국어로 억지로 옮기지 않는다 — `props`, `hook`, `migration` 처럼 그대로 쓰는 편이
통하는 말은 그대로 쓴다.
